
## Changes

Two styling updates on the Career variant (`/make-money-with-music`):

1. **"Student Wins" badge** (line 1364): Remove the green styling (`bg-[#D3FF02]/10 text-[#D3FF02]`) and replace with a neutral pill style (`border border-zinc-700 text-zinc-300 bg-zinc-900/50`), matching the eyebrow badge pattern used elsewhere.

2. **CTA button below bonuses** (line 645): Change from neon lime (`bg-[#D3FF02] text-black`) to white (`bg-white text-black hover:bg-zinc-200`), and remove the green glow shadow.

No other elements will be changed.
